# drop-typing — 产品需求文档（PRD）

> 版本：v0.5（产品定名 drop-typing）
> 日期：2026-07-22
> 状态：需求已确认，开发中
>
> 变更记录：
>
> - v0.5：产品正式定名 **drop-typing**（小写），原开发代号 Break Your Keyboard 废弃
> - v0.4：ASR 从 SiliconFlow 切换为阿里百炼 Qwen3-ASR-Flash（DashScope 接口，支持上下文偏置）；ASR Provider 抽象改为每家一个适配器；指令语义通道原则写入 4.3
> - v0.3：七个产品问题定稿（暂存条外观、波形反馈、指令机制、异常提示、语言检测等）；"Break Your Keyboard"确认为代号，正式名未定
> - v0.2：交互模型从"直接注入"改为"暂存区模式"；热键映射定稿；注入定为粘贴模式
> - v0.1：初版，三层功能定义、Tauri 选型、Provider 抽象

***

## 1. 产品定位

一款**按住说话、松手出字**的语音输入工具。语音识别（ASR）的结果不直接上屏，而是先经过大模型（LLM）清洗——修正标点、去除口水话、中英混排加空格、口语结构化——再注入到光标处。

除输入外，还提供两个语音通道：**语音修正**（对已输入内容打补丁）和**语音指令**（说"复制"即执行对应按键操作）。

项目以**开源**形式发布（License：MIT），所有模型与服务商均由用户自行配置。

> 注：产品正式名为 **drop-typing**（Break Your Keyboard 为已废弃的开发代号）。

## 2. 目标用户与场景

- 长时间打字、希望减轻键盘负担的重度文字工作者
- 用口述方式写文档、消息、代码注释的开发者
- 需要中英混杂输入（技术词汇多）的中文用户

## 3. 核心交互模型：暂存区模式（定稿）

### 3.1 模式概述

所有语音输入**不直接打进用户的编辑器**，而是先进入屏幕底部的一个**暂存条**（staging bar）。用户在暂存条中确认内容无误后，再一次性提交到光标处。

```
语音 → ASR → LLM 清洗 → 底部暂存条（可审阅/可编辑/可语音修正）
                                ↓ 短按右 ⌘ 提交
                     写入剪贴板 → 粘贴到当前光标处
```

### 3.2 为什么用这个模式（设计理由）

- **修正通道彻底解放**：语音修正改的是暂存条里的文本（自己的地盘），无需去用户编辑器里"选中旧文本再替换"——避免了回删误伤用户手改内容的不可逆风险
- **LLM 漂移有人肉关卡**：清洗结果先过眼再提交，大模型"顺手乱改"不会污染正式文档
- **清空零破坏**：清空的是暂存条，不动已上屏内容
- **剪贴板只在提交瞬间碰一次**，竞态窗口最小
- **附带能力：一次起草、多处粘贴**——暂存内容可粘贴到任意 App

### 3.3 暂存条规范（定稿）

| 项      | 规范                                                           |
| ------ | ------------------------------------------------------------ |
| 形态     | 屏幕底部浮层，**无焦点面板**（macOS non-activating panel），绝不抢用户当前 App 的焦点 |
| 外观     | **多行自适应**高度；位置**底部居中**；**深浅色跟随系统**                           |
| 追加规则   | 多次长按录音，清洗结果**追加**在已有内容之后                                     |
| 手动编辑   | 暂存条本质是文本框，**允许键盘直接编辑**                                       |
| 提交     | 短按右 ⌘：内容 → 剪贴板 → 粘贴到**当前聚焦 App** 的光标处，提交后暂存条清空               |
| 自动确认   | 暂存条旁有**开关**：开启后，松手 N 秒无操作自动提交（N 可在设置中调整）                     |
| 清空/丢弃  | 暂存条上提供清空按钮（放弃当前内容，不提交）                                       |
| 录音反馈   | **波形动画**；**无提示音**                                            |
| 异常提示   | 暂存条**整体变为黄色背景**，错误信息以**红色文字**显示在条内（如无网络 / Key 失效 / 额度耗尽）     |
| 切换 App | 起草中途切换 App，提交时贴到新 App——视为特性（一处起草、多处粘贴）                       |

### 3.4 长短按时序

同一个右 ⌘ 上叠两个动作，**判定放在松手时**：

- 按下时长 < 250ms → 短按：提交
- 按下时长 ≥ 250ms → 长按：录音（松开后触发 ASR + 清洗）

防误触：250ms 阈值可挡掉打字时顺带扫过修饰键的情况；误提交有"已提交"视觉反馈，且内容在光标处可正常撤销（Cmd+Z），损失可控。

## 4. 三层功能

### 4.1 第一层：语音输入 + 大模型清洗

**流程**：长按右 ⌘ 录音 → 松开 → ASR 转写 → LLM 清洗 → 追加到暂存条。

**识别语言**：自动检测中英文（ASR 层 auto，不暴露手动选项）。

清洗要求：

| 规则     | 说明                      | 实现方式           |
| ------ | ----------------------- | -------------- |
| 标点修正   | 逗号、句号等标点正确              | LLM            |
| 去口水话   | 去除"嗯""那个""就是说"等填充词      | LLM            |
| 中英混排空格 | 中文与英文/数字之间加空格（pangu 风格） | LLM 为主，正则后处理兜底 |
| 口语结构化  | "第一怎样、第二怎样" → 列表等结构化输出  | LLM            |

**优化强度档位**（设置中可调）：

- **保守**：只加标点，不改语序措辞
- **标准**（默认）：去口水词 + 标点 + 中英空格 + 适度结构化

### 4.2 第二层：语音修正（纠错通道）

**典型场景**：说 "Claude" 被识别成"云"。用户长按右 ⌥ 说："我说的 Claude，是 AI 公司 Anthropic 的模型 Claude。"

**流程**：长按右 ⌥ 录修正语音 → ASR 转写 → 将「暂存条当前全文 + 修正说明」一并发给 LLM → 输出修正后全文 → **整体替换暂存条内容**。

**设计要点**：

- 采用"全文重生成"方案（非局部替换），天然支持复杂修正指令："把第二点的 cloud 改成 Claude""语气改随意一点"
- **修正作用域 = 暂存条全部内容**（暂存区模式无回删风险）
- 提交后暂存条清空，修正作用域随之重置
- 不做专门的"撤销修正"入口：已提交内容靠用户编辑器的 Cmd+Z，暂存条内靠手动编辑/再次修正（已确认）

### 4.3 第三层：语音指令（定稿）

**流程**：长按右 ⇧ 录音 → 松开 → ASR 转写 → 意图判断 → 直接执行（不经过暂存条）。

**核心原则：指令语义完全来自右 ⇧ 通道本身，与语音内容无关。**
同一句"Shift Command E"，长按右 ⌘（输入通道）说出 = 普通文字，原样进暂存条（用户可能正在写一段讲快捷键的文档）；长按右 ⇧（指令通道）说出 = 解析成按键事件执行。ASR 转写出的文本在任何其他通道都不具有"指令特权"。

**指令通道内的两类指令**：

**A. 内置别名指令**（MVP 仅三条，本地映射表）：

| 用户说             | 执行动作     |
| --------------- | -------- |
| "复制" / "Ctrl+C" | 模拟 Cmd+C |
| "粘贴" / "Ctrl+V" | 模拟 Cmd+V |
| "回车" / "换行"     | 模拟 Enter |

**B. 组合键直说**：用户主动说出修饰键组合，如"Shift+Command+E"→ 解析为对应按键事件直接执行。覆盖任意快捷键，无需预置映射表。

解析规则（本地，不经过 LLM）：识别 `shift / command / control / option / alt` 等修饰词 + 尾部按键名（字母、数字、F 键、方向键、空格、Tab、Esc 等），转换为按键事件。

**设计要点**：

- 全部走**本地匹配/解析**，延迟最低
- 未命中时 fallback：提示【未识别到按键指令】
- MVP 采用整段判断，不处理"指令 + 内容"混合语句

## 5. 热键方案（定稿）

| 功能        | 热键                | 短按（<250ms） | 长按（≥250ms）    |
| --------- | ----------------- | ---------- | ------------- |
| 语音打字 / 提交 | **右 Command (⌘)** | 提交暂存条到光标处  | 录音输入，结果追加到暂存条 |
| 语音优化      | **右 Option (⌥)**  | —（无动作）     | 录修正语音，重写暂存条   |
| 语音控制      | **右 Shift (⇧)**   | —（无动作）     | 录指令语音，直接执行    |

**选型理由**：

- macOS 系统快捷键不区分左右修饰键，**单独按住修饰键不触发任何系统行为**——裸右修饰键零冲突
- 否决 Fn 方案：Fn 在 Windows 上对操作系统不可见（键盘固件处理），跨平台死路；macOS 上 Tauri 热键插件也不支持
- 已避开的系统占用：⌘Space、⌃Space、⌘Tab、⌃←/→、⌘⌥Esc、F11/F12、⌘⇧3/4/5、⌃⌘Q

**已知风险**：第三方软件理论上可能单独绑定右 ⌘（极少见），250ms 阈值可挡掉绝大多数连带触发；冲突时用户可换备选预设。

**备选预设**：⌃⌥1（输入）/ ⌃⌥2（修正）/ ⌃⌥3（指令），系统无占用。

**可配置**：所有热键在设置中可重录，配置文件中为字符串；跨平台时 Windows 侧映射到 Right Win / Right Alt / Right Shift。

## 6. 技术方案

### 6.1 总体架构

```
麦克风 → ASR（语音识别）→ LLM 清洗 / 意图分流 → 暂存条 →（提交时）剪贴板 → 光标处
```

### 6.2 框架：Tauri

- **选型理由**：包体小（约 10MB）、内存低、Rust 侧音频（cpal）与全局热键生态成熟、对开源贡献者友好
- 平台：MVP 只做 macOS；音频、热键、注入三处平台相关代码做抽象，保证 Windows 可移植
- 备选：Electron（开发更快但包体大、原生模块在 Apple Silicon 有坑）

### 6.3 文字注入：粘贴模式（定稿）

```
提交时：暂存条文本 → 写入系统剪贴板 → 模拟 Cmd+V → 恢复原剪贴板
```

- macOS 按键事件无法表达汉字（汉字需 IME 组词），模拟按键模式**已否决并从路线图删除**
- 粘贴模式中英文通吃、瞬时完成；剪贴板只在提交瞬间使用

### 6.4 Provider 抽象层（开源核心设计）

模型与服务商全部用户可配置，不作为事后补丁。

**LLM Provider**：

- 统一走 OpenAI 兼容协议，仅需配置 `base_url + api_key + model_name`
- 一套实现兼容 DeepSeek / OpenAI / Qwen / Ollama（本地）
- MVP 默认配置：DeepSeek

**ASR Provider**：

- 抽象层定义统一转写接口，**每家服务商一个适配器**（各家 ASR API 格式差异大，不做单一协议假设）
- MVP 默认：**阿里百炼 Qwen3-ASR-Flash**（已确认，替代此前的 SiliconFlow 方案）
  - DashScope multimodal-generation 同步接口，音频以 base64 data URL 直传，适合按住说话的短音频
  - 11 种语言自动检测，中英句中切换专门优化（中文错误率 3.97%，优于 GPT-4o Transcribe）
  - 支持**上下文偏置**：可随请求传热词表/背景段落（如暂存条现有文本、用户词表），在 ASR 层缓解专业词误识别（如 "Claude" → "云"）
- 备选：Fun-ASR（中英自由切换、噪声鲁棒）；后续流式版本接 qwen3-asr-flash-realtime（WebSocket）
- 预留本地模式接口（后续接 whisper.cpp），对应"极致隐私"模式

### 6.5 配置与隐私

- 配置文件：本地 `config.toml`，含热键、provider、优化强度、自动确认
- API Key 仅存本地，绝不上传；README 中明确声明
- 本地模式（后续）：ASR 与 LLM 均可完全离线，满足极致隐私需求

### 6.6 macOS 权限

- **辅助功能（Accessibility）权限**：全局热键监听与按键模拟所需
- **麦克风权限**：录音所需
- 首次启动提供权限引导流程，未授权时给出清晰提示

## 7. MVP 范围

### 包含

- macOS 端 Tauri 应用
- 暂存条（无焦点浮层、多行自适应、底部居中、深浅色跟随、追加、手动编辑、清空按钮、自动确认开关、波形动画、黄色异常态）
- 三个右修饰键热键（长短按时序、可配置）
- 云端 ASR（阿里百炼 Qwen3-ASR-Flash，DashScope 同步接口，中英自动检测，支持热词/上下文偏置）
- LLM 清洗（DeepSeek，OpenAI 兼容协议）+ 优化强度档位
- 语音修正通道（暂存条全文重生成）
- 语音指令（三条内置别名 + 组合键直说解析 + fallback 追加到暂存条）
- 粘贴模式提交
- 设置界面：provider 配置、热键配置、优化强度、自动确认
- 权限引导

### 不包含（后续版本）

- Windows 支持
- 本地 ASR（whisper.cpp）/ 极致隐私模式
- 流式清洗（边识别边进暂存条）
- 混合语句拆分（指令 + 内容序列执行）
- 指令别名的自定义编辑界面（组合键直说已覆盖大部分需求）
- LLM 意图判断 fallback

## 8. 里程碑

| 阶段 | 内容                                                                   | 验收标准                                 |
| -- | -------------------------------------------------------------------- | ------------------------------------ |
| M1 | Tauri 脚手架 + 暂存条浮层 + 长按右⌘录音 → 百炼 Qwen3-ASR-Flash 转写 → 进暂存条（不清洗）+ 短按提交 | 能说话进暂存条、短按贴到光标处                      |
| M2 | 接入 DeepSeek 清洗层 + 优化强度档位                                             | 标点/口水话/中英空格/结构化生效                    |
| M3 | 修正通道（右⌥）+ 手动编辑 + 自动确认开关                                              | 说"Claude 不是云"能重写暂存条                  |
| M4 | 指令通道（右⇧，别名 + 组合键直说）+ 设置界面 + 权限引导                                     | "复制"执行 Cmd+C；"Shift+Command+E"正确解析执行 |
| M5 | 打磨 + 开源发布（README、License、Issue 模板）                                   | 仓库公开，他人可配自己的 Key 跑起来                 |

## 9. 已确认产品决策汇总

| 问题         | 决策                                               |
| ---------- | ------------------------------------------------ |
| 右 ⌥ 短按撤销修正 | **不做**（用户用编辑器自带 Cmd+Z）                           |
| 暂存条外观      | 多行自适应、底部居中、深浅色跟随系统                               |
| 录音反馈       | 波形动画，无提示音                                        |
| 指令表        | 内置仅复制/粘贴/回车；其余靠"组合键直说"（如 Shift+Command+E）        |
| 识别语言       | 自动检测中英文                                          |
| 异常提示       | 暂存条整体黄色背景 + 红色错误文字                               |
| 产品名        | 正式定名 **drop-typing**（原代号 Break Your Keyboard 废弃） |

## 10. 风险

| 项             | 说明                                                                         |
| ------------- | -------------------------------------------------------------------------- |
| 端到端延迟         | 松手 → 进暂存条目标 < 2s；取决于 ASR 与 LLM 首 token 速度，需实测百炼 Qwen3-ASR-Flash + DeepSeek |
| 无焦点面板兼容性      | non-activating panel 在部分全屏 App / 多桌面场景下行为需实测                               |
| 热键冲突          | 第三方软件可能占用右修饰键，故必须可配置 + 备选预设                                                |
| ASR 专业词识别     | "Claude → 云"类错误已有两道防线：Qwen3-ASR-Flash 的上下文偏置（热词表/暂存条文本随请求传入）+ 语音修正通道兜底     |
| 组合键解析的 ASR 误差 | "Shift+Command+E"经 ASR 转写可能变形（如"shift command e"），解析器需容忍大小写/空格/连字符变体       |
| 成本控制          | LLM 清洗每次输入都调用，需提供关闭清洗/ASR 直出的选项                                            |

